- defaults:
    brew:
        stdin: true
        stderr: true
        stdout: true

    cask:
        stdin: true
        stderr: true
        stdout: true

    brewfile:
        stdin: true
        stderr: true
        stdout: true

- install-brew: True

- brewfile:
  - Brewfile-example

- brew:
  - git
  - bash

- tap:
    - homebrew/cask-fonts

- brew: [ack, ag]

- cask:
  - gpg-suite
  - vlc
